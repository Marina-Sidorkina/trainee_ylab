{
  "compilerOptions": {
    "target": "ES5", // Версия стандарта ECMAScript, в которую будет скомпилирован код
    "module": "ES6", // Модульная система, которую будет использовать собранное приложение
    "moduleResolution": "node", // Стратегия, кот. будет исп-ся для импорта модулей (node или classic)
    "baseUrl": "./",
    "outDir": "./dist/", // Конечная папка, куда будут помещаться собранные артефакты (.js, .d.ts, и .js.map файлы)
    "paths": { "@src/*": ["src/*"] },
    "strict": true,
    "noImplicitAny": true, // Запрет на нетипизированные сущности
    "allowJs": true,
    "allowSyntheticDefaultImports": true, // вместо -import * as React from 'react'- можно -import React from 'react';-
    "forceConsistentCasingInFileNames": true, // Включает режим чувствительности к регистру для импорта файлов
    "noFallthroughCasesInSwitch": true, // Флаг проверяет наличие break в операторе switch/case:
    "resolveJsonModule": true, // Возможность импортировать *.json файлы
    "noEmit": false, // Т.к. не удален babel
    "jsx": "react-jsx" // Поддержка jsx
  },
  "include": ["src/*"], // В каких папках искать файлы
  "exclude": ["node_modules", "dist"] // Исключить из поиска файлы из данных папок
}
